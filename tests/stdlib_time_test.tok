# Standard library: @"time" module tests

t=@"time"

# --- now() ---
ts=t.now()
pl(type(ts))
pl(ts>0)

# --- fmt(ts, pattern) ---
# Use a known timestamp: 2024-01-15 12:30:45 UTC = 1705321845
pl(t.fmt(1705321845 "%Y-%m-%d %H:%M:%S"))

# --- sleep(ms) ---
start=clock()
t.sleep(50)
elapsed=clock()-start
pl(elapsed>=50)
